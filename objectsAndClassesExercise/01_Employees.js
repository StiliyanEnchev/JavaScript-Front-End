function solve(arr) {
    let obj = []
    
    for (row of arr) {
        console.log(`Name: ${row} -- Personal Number: ${row.length}`)
    }
}

solve([
    'Silas Butler',
    'Adnaan Buckley',
    'Juan Peterson',
    'Brendan Villarreal'
    ]
    )